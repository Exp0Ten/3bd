use toml;

use crate::window;
use crate::data::*;

/*
pub struct Config {
    name: String,
    window: Settings,
    theme: Theme,
    layout: Layout,
    keybinds: Keybinds,
}

struct Layout {

}

struct Keybinds {

}

impl Config {
    fn new(name: &str, ) -> Self {

    }
}

impl Default for Config {
    fn default() -> Self {
        
    }
}


impl Default for Layout {
    fn default() -> Self {
        
    }
}

impl Default for Keybinds {
    fn default() -> Self {
        
    }
}
*/